services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: anislanding-app
    container_name: anislanding-app
    ports:
      - "3369:3369"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_MAIN_URL=${NEXT_PUBLIC_MAIN_URL:-http://localhost:3369}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3369"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Development service
  dev:
    build:
      context: .
      dockerfile: Dockerfile
      target: builder
    container_name: anislanding-dev
    ports:
      - "3369:3369"
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_MAIN_URL=${NEXT_PUBLIC_MAIN_URL:-http://localhost:3369}
    command: npx next dev -H 0.0.0.0 -p 3369
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
    profiles:
      - dev
